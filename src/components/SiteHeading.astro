---
import { cn } from "@/lib/utils";
import Header from "./Header.astro";

const { class: className = "", ...extra } = Astro.props;
---

<div class={cn("container max-w-7xl", className)} {...extra}>
	<div class="h-full w-full">
		<Header />
		<div class="mt-6 md:mt-12">
			<slot />
		</div>
	</div>
</div>
