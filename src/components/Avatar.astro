---
import profile_pict from "@/images/me.jpg";
import { Image } from "astro:assets";

interface Props {
	alt: string;
	class?: string;
}

const { alt, class: className = "" } = Astro.props;
---

<Image src={profile_pict} alt={alt} class={className} />
