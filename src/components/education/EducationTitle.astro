---
interface Props {
	institute: string;
	status?: "current" | "archive";
	class?: string;
}

const { institute, status = "archive", class: className = "" } = Astro.props;

const statusConfig = {
	current: { label: "INFO:", color: "text-green-500" },
	archive: { label: "ARCHIVE:", color: "text-blue-500" },
};

const { label, color } = statusConfig[status];
---

<div class:list={["flex items-center gap-2", className]}>
	<span class:list={["font-bold", color]}>{label}</span>
	<h3 class="text-content text-2xl font-extrabold tracking-tight uppercase">
		{institute}
	</h3>
</div>
