---
interface Props {
	class?: string;
}

const { class: className = "" } = Astro.props;
---

<div
	class:list={[
		"hover:bg-primary/5 border-border gap-3 border-b p-6 transition-colors ",
		className,
	]}>
	<div class="flex flex-col gap-4 md:contents">
		<slot />
	</div>
</div>
